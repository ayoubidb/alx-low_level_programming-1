#include "main.c"

/**
 * print_chessboard - print a chessboard.
 * @a: source matrix
 *
 */

void print_chessboard(char (*a)[8])
{
	int row, col;

	for (row = 0; row <= 7; row++)
	{
		for (col = 0; col <= 7; col++)
			_putchar(a[row][col]);

		_putchar('\n');
	}
}
